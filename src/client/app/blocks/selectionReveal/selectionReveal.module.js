import angular from 'angular';
import selectionReveal from './components/selectionReveal.component';

angular
    .module('blocks.selectionReveal', [])
    .component('selectionReveal', selectionReveal);

