import template from './logoutButton.component.html';

const logoutButton = {
    bindings: {
    },
    controller: [controller],
    templateUrl: template
};

function controller() {
    var vm = this;
}

export default logoutButton;

