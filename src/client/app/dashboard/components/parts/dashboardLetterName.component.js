import template from './dashboardLetterName.component.html';

const DashboardLetterName = {
    bindings: {
        letter: '<'
    },
    controllerAs: 'vm',
    templateUrl: template
};

export default DashboardLetterName;

