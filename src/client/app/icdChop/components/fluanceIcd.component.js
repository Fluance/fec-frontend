import template from './fluanceIcd.component.html';

const Icd = {
    templateUrl: template,
    controller: function () {
    },
    bindings: {
        result: '<',
        preferredLanguage: '<?'
    }
};

export default Icd;
