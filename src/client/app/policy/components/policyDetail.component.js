import template from './policyDetail.component.html';

const PolicyDetail = {
    bindings: {
        policy: '<'
    },
    controller: function () {
    },
    templateUrl: template
};

export default PolicyDetail;
