import template from './fecPatientVisitLocation.component.html';

const PatientVisitLocation = {
    bindings: {
        visit: '<'
    },
    controller: [controller],
    templateUrl: template
};

function controller() {}

export default PatientVisitLocation;
