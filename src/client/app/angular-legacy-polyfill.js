// ==============================================================================
// Polyfills for 3rd-party libraries that require legacy AngularJS behavior
// ==============================================================================
angular.lowercase = function(text) {
    return text.toLowerCase();
};
