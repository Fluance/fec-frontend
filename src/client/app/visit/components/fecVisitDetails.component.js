import template from './fecVisitDetails.component.html';

const VisitDetails = {
    bindings: {
        details: '<'
    },
    controller: [controller],
    templateUrl: template
};

function controller() {}

export default VisitDetails;
