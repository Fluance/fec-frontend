import template from './fecVisitPhysicianAdmitting.component.html';

const VisitPhysicianAdmitting = {
    bindings: {
        physician: '<'
    },
    controller: [controller],
    templateUrl: template
};

function controller() {}

export default VisitPhysicianAdmitting;
